apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: fiware-platform
data:
  cratedb.yaml: |
    apiVersion: 1
    datasources:
      - name: CrateDB
        type: postgres
        uid: CrateDB              # 👈 CRITIQUE : Force l'ID utilisé dans le Dashboard
        url: cratedb:5432
        user: crate
        database: doc
        access: proxy
        isDefault: true
        editable: true
        jsonData:
          sslmode: "disable"      # Désactive SSL (CrateDB interne)
          postgresVersion: 1200   # 👈 AIDE : Dit à Grafana "Considère que c'est du PG 12"
          database: "doc"
        secureJsonData:
          password: ""            # Mot de passe vide explicite