apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-agent
  namespace: fiware-platform
  labels:
    app: weather-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: weather-agent
  template:
    metadata:
      labels:
        app: weather-agent
    spec:
      containers:
        - name: weather-agent
          image: python:3.11-slim
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8888
          env:
            - name: WEATHER_APIS_CONFIG
              value: '[{"type": "script", "name": "LocalScript-Primary", "path": "/opt/scripts/weather-data.sh"}, {"type": "openmeteo", "name": "Open-Meteo-Fallback"}]'
            - name: ORION_URL
              value: "http://orion:1026/v2/entities"
            - name: PORT
              value: "8888"
          volumeMounts:
            - name: scripts
              mountPath: /opt/scripts
              readOnly: true
          command: ["sh", "-c"]
          args:
            - pip install --no-cache-dir flask requests && python /opt/scripts/weather_agent.py
          livenessProbe:  
            httpGet:  
              path: /health  
              port: 8888  
            initialDelaySeconds: 30  
            periodSeconds: 10  
          readinessProbe:  
            httpGet:  
              path: /health  
              port: 8888  
            initialDelaySeconds: 10  
            periodSeconds: 5
      volumes:
        - name: scripts
          configMap:
            name: weather-agent-scripts
            defaultMode: 0755
