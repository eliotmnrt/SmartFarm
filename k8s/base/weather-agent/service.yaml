apiVersion: v1
kind: Service
metadata:
  name: weather-agent
  namespace: fiware-platform
  labels:
    app: weather-agent
spec:
  selector:
    app: weather-agent
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888
  type: ClusterIP
